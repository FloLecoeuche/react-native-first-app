import React from 'react';
import { View } from 'react-native';

const FlexSeparator = ({ flex }) => {
  return (
      <View 
        style={{ flex: flex }}
      />
  );
};

export { FlexSeparator };
